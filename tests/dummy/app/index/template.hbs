<div class="py-16">
  <section class="pb-20 mb-20 text-center">
    <h1 class="mb-2 px-4 text-3xl">Oooh! Building forms is my favorite!</h1>
    <em class="font-medium text-grey-dark italic">- said no one, ever</em>
  </section>

  <div class="container">
    <section class="mb-20">
      <WiredCard @class="markdown-card bg-green-lightest" @animated="true">
        {{markdown-to-html "`ember install ember-yup`"}}
      </WiredCard>
    </section>

    <section class="mb-8 flex">
      <div class="md:w-4/5 mx-auto">
        <div class="">
          <h2 class="md:text-center mb-12">How to validate?</h2>
          <div class="flex flex-col md:flex-row">
            <WiredCard @class="md:w-1/2 p-6 mb-6">
              <h3>
                Validate mixin
              </h3>
              <p>This mixin enhances models in 2 ways:</p>
              <ul>
                <li>{{markdown-to-html "Adds method: `validate()`" class="markdown"}}</li>
                <li>{{markdown-to-html "Adds save option: `save({ validate: true })`" class="markdown"}}</li>
              </ul>
              <footer class="mt-12">
                <p>{{link-to "See tutorial" "getting-started.ember-data"}}</p>
              </footer>
            </WiredCard>

            <WiredCard @class="md:w-1/2 p-6 mb-6">
              <h3>Build schemas</h3>
              <p>{{markdown-to-html "Import `yup` into your files and validate against them directly within your app" class="markdown"}}</p>
              <p>Provides more validation power and flexibility</p>
              <footer class="mt-12">
                <p>{{link-to "See tutorial" "getting-started.using-schemas"}}</p>
              </footer>
            </WiredCard>
          </div>
        </div>
      </div>
      <!-- <WiredCard @class="w-full md:w-1/3 p-6 mb-6">
        <h3>FormField components</h3>
        <p>wrap fields around your controls to validate their values</p>
        <p>the ValidationForm component supports both sync and async submission</p>
        <p>{{link-to "See examples" "getting-started.using-components"}}</p>
      </WiredCard> -->
    </section>

  </div>
</div>
